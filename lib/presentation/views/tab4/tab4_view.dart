import 'package:flutter/material.dart';

class Tab4View extends StatelessWidget {
  const Tab4View({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
        child: Text('Implementation basic provider state maangements'));
  }
}
